AppSettings = {
  // @if ENV == 'DEVELOPMENT'
  baseApiUrl: 'http://studmateapp.herokuapp.com/',
  // @endif
  // @if ENV == 'PRODUCTION'
  baseApiUrl: 'https://studmateapp.herokuapp.com/'
  // @endif
}